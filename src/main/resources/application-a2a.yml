spring:
  config:
    activate:
      on-profile: a2a

# The native Embabel A2A/MCP starters are activated by running with the "a2a"
# profile. Goal methods explicitly marked with @Export(remote = true)
# are exposed remotely over A2A (and MCP server integrations).
