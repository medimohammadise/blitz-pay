name: CD pipeline

on:
  release:
    types:
      - published

jobs:
  ci:
    permissions:
      actions: write
      contents: write
      checks: write
      pull-requests: write
    uses: ./.github/workflows/ci.yml
  release:
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    #needs: ci
    if: github.event_name == 'release'
    uses: elegant-software/elegant-ci-cd-pipeline/.github/workflows/backend-docker-build.yml@1f506dd1bd3686d6e31b3169977b2b672ff38a1a
    with:
      java-version: ${{ vars.JAVA_VERSION }}
      image-name: ${{ vars.APP_NAME }}
      image-tag: ${{ github.ref_name }}
      registry: ${{ vars.CONTAINER_REGISTRY }}
