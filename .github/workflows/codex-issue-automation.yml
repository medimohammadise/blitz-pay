name: Call Issue Spec Automation
on:
  repository_dispatch:
    types:
      - project_status_change
permissions:
  contents: write
  issues: write
  pull-requests: write
  repository-projects: write
jobs:
  call-automation:
    uses: elegant-software/elegant-ci-cd-pipeline/.github/workflows/codex-github-issue-spec-kit-automation.yml@main
    secrets: inherit